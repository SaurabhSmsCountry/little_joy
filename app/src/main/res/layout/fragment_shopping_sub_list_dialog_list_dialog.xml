<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.dashboard.onlineShopping.productCategory.ShoppingSubListDialogFragment"
    android:orientation="vertical"
    android:layout_margin="10dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        style="@style/TextStyle.ExtraBold"
        android:id="@+id/titleSubCategory"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="20dp"
        android:layout_marginLeft="10dp"
        android:text="@string/featured_category"
        android:layout_weight="1"
        android:textColor="@color/textColorDark"
        android:textSize="20sp" />

<GridView
    android:id="@+id/list"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:horizontalSpacing="10dp"
    android:numColumns="3"
    android:verticalSpacing="10dp"
    android:layout_weight="2"
    android:paddingTop="@dimen/list_item_spacing_half"
    android:paddingBottom="@dimen/list_item_spacing_half"
    tools:listitem="@layout/fragment_shopping_sub_list_dialog_list_dialog_item" />

</LinearLayout>